# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Chess Timer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  pod 'ChromaColorPicker'
  pod 'GhostTypewriter'
  pod "SAConfettiView"
  use_frameworks!

  # Pods for Chess Timer

  target 'Chess TimerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Chess TimerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
